/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['yt3.ggpht.com', 'img.freepik.com', 'cdn-icons-png.flaticon.com', 'localhost']
    }
}

module.exports = nextConfig
